import React from 'react';
import Title from './Title'


export default {
    title: 'Components',
    component: Title
}

export const storyTitle = () => <Title />;

storyTitle.story = {
    name: 'Title',
  };
